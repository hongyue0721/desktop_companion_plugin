# 🖥️ MaiBot 桌面伴侣插件

> 让 MaiBot 感知你的桌面环境，支持日程管理、定时提醒与截图记录。

## ✨ 功能特性

- **📅 日程管理**：通过命令添加/查看日程，到期自动提醒
- **⏰ 定时提醒**：每天 09:00 早安、23:00 晚安问候
- **📸 定时截图**：每 30 分钟自动截屏并记录（可配置）

## 📦 安装

1. 将本文件夹复制到 MaiBot 的 `plugins` 目录
2. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
3. 重启 MaiBot

## 🎮 使用方法

### 命令列表

| 命令 | 说明 | 示例 |
|------|------|------|
| `/add_event <时间> <内容>` | 添加日程 | `/add_event 2026-02-01 22:00 开会` |
| `/list_events` | 查看当前日程 | `/list_events` |

### 时间格式

- 固定格式：`YYYY-MM-DD HH:MM`
- 示例：`2026-02-01 14:30`

## ⚙️ 配置说明

通过 MaiBot WebUI 或编辑 `config.toml` 配置：

```toml
[plugin]
enabled = true  # 是否启用插件

[schedule]
event_check_seconds = 10  # 日程检查间隔（秒）

[screenshot]
enabled = true           # 是否启用定时截图
interval_minutes = 30    # 截图间隔（分钟）
cleanup_file = true      # 截图后是否删除临时文件

[target]
default_stream_id = ""   # 默认推送 stream_id
```

## 📁 文件结构

```
desktop_companion_plugin/
├── _manifest.json      # 插件清单
├── plugin.py           # 插件主逻辑
├── config.example.toml # 配置示例
├── requirements.txt    # 依赖列表
├── README.md           # 本文件
└── LICENSE             # 开源协议
```

## 📋 依赖

- `pyautogui` - 截图功能

## 🔗 相关项目

- [MaiBot-Desktop-Adapter](../desktop_adapter/) - 桌面适配器
- [MaiBot-Desktop-UI](../desktop_ui/) - 桌面 UI

## 📄 许可证

MIT License
